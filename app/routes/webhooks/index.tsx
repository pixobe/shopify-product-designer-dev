
export const loader = async () => {
    return { success: false, message: "No webhook specified" };
};

export const action = async () => {
    return { success: false, message: "No webhook specified" };
};
